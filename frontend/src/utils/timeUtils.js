// export const FPS = 30; // Standard frame rate assumption

// export const formatTime = (seconds) => {
//   const date = new Date(seconds * 1000);
//   const mm = date.getUTCMinutes();
//   const ss = date.getUTCSeconds();
//   const ms = date.getUTCMilliseconds();
//   return `${mm.toString().padStart(2, '0')}:${ss.toString().padStart(2, '0')}:${ms.toString().padStart(3, '0').slice(0, 2)}`;
// };

// export const secondsToFrame = (time) => Math.floor(time * FPS);